#ifndef SHOWLIST_H
#define SHOWLIST_H
#include <gtk/gtk.h>

void add_file_to_playqueue(char*);
char* live_action(char*);
char* anime(char*);
char* other(char*);
char* std_anime(char*,char*);

#endif
