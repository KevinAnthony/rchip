themedir = $(datadir)/icons/hicolor
size = scalable
context = apps

iconsdir = $(themedir)/$(size)/$(context)

icons_DATA = \
	rchip_server.svg

noinst_DATA =

EXTRA_DIST = $(icons_DATA)

CLEANFILES = Makefile
