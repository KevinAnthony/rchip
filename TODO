
	* msdaemon prints something when _silent, when adding a file
		Kevin

	* replace _DEBUG _SILENT with verbosity
		Kevin

	* gconf settings for desthostname in notification.c setNotification
		Kevin

	* some popup window to set desthostname, from registered devices perhaps
		Kevin

	* notificaiton.c source_hostname hardcoded
		Kevin

	* mstray open window should open to VIDEO_PATH instead of random dir
		Kevin
	
	* Make callback functions under settings.c
		Kevin
